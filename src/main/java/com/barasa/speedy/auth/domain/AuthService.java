package com.barasa.speedy.auth.domain;

// Authentication Logic
// Login Handler. This service is used during the login process (/api/auth/login). It receives credentials, verifies the user via the UserService, and then delegates to JwtTokenService to create the JWT string upon successful authentication.
public class AuthService {

}
