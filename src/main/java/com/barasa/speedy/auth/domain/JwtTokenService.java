package com.barasa.speedy.auth.domain;

// Token Utility
// Creation, Signing, Validation, Expiration. This service holds the logic to sign (encrypt) the token using the secret key and to read (decrypt) and validate the claims (like sub/uuid) from an incoming token.
public class JwtTokenService {

}
